# Allow sudo to pass the OMADA_USER environment variable to control.sh/tpeap
Defaults:omada  env_keep += "OMADA_USER"
# Allow the omada user to run Omada control scripts as root
omada           ALL=(root) NOPASSWD:/opt/tplink/EAPController/bin/control.sh,/usr/bin/tpeap
